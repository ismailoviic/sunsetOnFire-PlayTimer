import { createUseStyles } from 'react-jss'

export const useStyles = createUseStyles({
  costDiv: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },
  cost: {
    fontFamily: 'Cooper',
    fontSize: '30px',
    fontWeight: 'bold',
    color: 'white',
    marginTop: '0px',
    marginBottom: '5px'
  }
})
