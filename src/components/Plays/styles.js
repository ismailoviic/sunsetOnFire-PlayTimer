import { createUseStyles } from 'react-jss'

export const useStyles = createUseStyles({
  playsDiv: {
    display: 'flex',
    justifyContent: 'center',
    alineItems: 'center',
    flexDirection: 'row',
    flexWrap: 'wrap'
  }
})
